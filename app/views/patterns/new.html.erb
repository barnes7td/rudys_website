<%= form_for @pattern , :url => { action: "create" }, :html => {:multipart => true} do |f| %>
  
  <p><%= f.label :title %></p>
  <p><%= f.text_field :title %></p>

  <p><%= f.label :description %></p>
  <p><%= f.text_area :description %></p>

  <p><%= f.file_field :image %></p>

  <%= f.submit "Add" %>
  
<% end %>
